version: \'3\'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      - FLASK_APP=src.main
      - FLASK_DEBUG=1
      - FLASK_RUN_HOST=0.0.0.0

